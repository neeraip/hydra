--- crates/engine-uds/src/io/rain.rs
+++ replace && with || in parse_archive_file
@@ -425,17 +425,17 @@
                     if !absent && value > 0 {
                         let t = at - interval;
                         readings.push((archive_day(date, t), value as f64 / 100.0));
                     }
                 }
             }
         }
     }
-    if readings.is_empty() && notices.is_empty() {
+    if readings.is_empty() || /* ~ changed by cargo-mutants ~ */ notices.is_empty() {
         return Err(format!(
             "archival station record for {station:?} holds no rainfall at all"
         ));
     }
     Ok((
         crate::io::iface::RainGageRecord {
             station,
             interval,
