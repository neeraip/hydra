--- crates/engine-uds/src/io/rain.rs
+++ delete match arm '}' | ']' in parse_archive_file
@@ -380,17 +380,17 @@
                 break;
             };
             if hour >= 25 {
                 break;
             }
             let condition = match flag {
                 'a' | 'A' => Some(false),
                 '{' | '[' => Some(true),
-                '}' | ']' => Some(false),
+                 /* ~ changed by cargo-mutants ~ */
                 _ => None,
             };
             if let Some(open) = condition {
                 if flag == '{' || flag == '[' {
                     missing_period = true;
                 } else if !open {
                     missing_period = false;
                 }
