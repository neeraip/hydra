--- crates/engine-uds/src/io/rain.rs
+++ delete match arm 'a' | 'A' in parse_archive_file
@@ -378,17 +378,17 @@
             k += width;
             let Some((hour, minute, value, flag)) = archive_group(group, layout) else {
                 break;
             };
             if hour >= 25 {
                 break;
             }
             let condition = match flag {
-                'a' | 'A' => Some(false),
+                 /* ~ changed by cargo-mutants ~ */
                 '{' | '[' => Some(true),
                 '}' | ']' => Some(false),
                 _ => None,
             };
             if let Some(open) = condition {
                 if flag == '{' || flag == '[' {
                     missing_period = true;
                 } else if !open {
