--- crates/engine-uds/src/io/rain.rs
+++ replace * with + in parse_rain_file
@@ -62,17 +62,17 @@
         let value = t[6]
             .parse::<f64>()
             .ok()
             .filter(|v| v.is_finite())
             .ok_or_else(|| bad(&format!("value {:?} is not a number", t[6])))?;
         readings.push(RainReading {
             station: t[0].to_string(),
             date: Date { year, month, day },
-            seconds: f64::from(hour) * 3600.0 + f64::from(minute) * 60.0,
+            seconds: f64::from(hour) + /* ~ changed by cargo-mutants ~ */ 3600.0 + f64::from(minute) * 60.0,
             value,
         });
     }
     Ok(readings)
 }
 
 #[cfg(test)]
 mod tests {
