[package]
name = "hydra-sdk"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
repository.workspace = true
homepage.workspace = true
documentation.workspace = true
description = "Water infrastructure simulation — EPANET-compatible extended-period water distribution simulation"
readme = "README.md"
keywords = ["epanet", "water", "hydraulics", "simulation", "network"]
categories = ["science", "simulation"]
publish = true

[dependencies]
hydra-common = { path = "../common", version = "5.1.0" }
hydra-engine-wds = { path = "../engine-wds", version = "5.1.0" }
hydra-engines = { path = "../engines", version = "5.1.0" }
hydra-report = { path = "../report", version = "5.1.0" }

[features]
# Enables the PDF renderer in hydra::report (heavyweight: embeds Typst).
report-pdf = ["hydra-report/pdf"]
