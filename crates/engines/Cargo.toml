[package]
name = "hydra-engines"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
authors.workspace = true
license.workspace = true
publish = true
repository.workspace = true
homepage.workspace = true
documentation.workspace = true
description = "Hydra engine dispatch — routes a model of unknown provenance to the engine that owns it"
readme = "README.md"
keywords = ["water", "hydraulics", "simulation", "engine"]
categories = ["science", "simulation"]

[dependencies]
hydra-common = { path = "../common", version = "15.0.0" }
hydra-engine-uds = { path = "../engine-uds", version = "15.0.0" }
hydra-interop-swmm = { path = "../interop-swmm", version = "15.0.0" }
hydra-interop-epanet = { path = "../interop-epanet", version = "15.0.0" }
hydra-engine-wds = { path = "../engine-wds", version = "15.0.0" }
